<UserControl x:Class="ModuleA.Views.ViewC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Text="溫馨提示"></TextBlock>
        <TextBlock Grid.Row="1" Text="大家好"></TextBlock>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="確定" Command="{Binding ConfirmCmm}"></Button>
            <Button Content="取消" Command="{Binding CancelCmm}"></Button>
        </StackPanel>
    </Grid>
</UserControl>